{
  "name": "s2c-evals",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Eval harness for the screenshot-to-calendar AI pipeline",
  "scripts": {
    "generate": "tsx src/generate-posters.ts",
    "eval": "tsx src/harness.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright-core": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
