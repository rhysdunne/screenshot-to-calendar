{
  "name": "s2c-infra",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CDK app for screenshot-to-calendar (staging + prod)",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "synth": "cdk synth",
    "deploy:staging": "cdk deploy S2cWeb-staging S2cBackend-staging",
    "deploy:prod": "cdk deploy S2cWeb-prod S2cBackend-prod"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.261.0",
    "constructs": "^10.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "aws-cdk": "^2.1100.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
